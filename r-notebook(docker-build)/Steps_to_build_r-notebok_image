Build the base notebook using ubuntu as a base image in dockerfile
Once the base notebook is successfully built, Push the base notebook image to DockerHub

Build the minimal notebook using base-notebook as a base image in dockerfile
Once the minimal notebook is successfully built, push the minimal notebook image to DockerHub

Build the r-notebook image using minimal notebook as a base image in dockerfile
Once the r-notebook image is successfully built, Push that image to dockerhub
